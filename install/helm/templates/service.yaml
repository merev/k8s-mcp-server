apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: k8s-mcp-server
